image: python:3.9

variables:
  SITE_URL: "https://your-gitlab-pages-url.com" # Update with your GitLab Pages URL

before_script:
  - pip install -U pip  # Upgrade pip
  - pip install -r requirements.txt  # Install necessary dependencies

stages:
  - build
  - deploy

update_index:
  stage: build
  script:
    - ./update.sh

pages:
  stage: deploy
  script:
    - "echo deploying!"
  artifacts:
    paths:
      - public
  only:
    variables:
      - $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
