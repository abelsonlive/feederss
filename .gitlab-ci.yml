image: python:3.9

before_script:
  - make install

pages:
  stage: deploy
  script:
    - make build
  artifacts:
    paths:
      - public
  only:
    variables:
      - $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
